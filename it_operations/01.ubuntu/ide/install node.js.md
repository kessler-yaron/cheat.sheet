# install node.js

## General Information
* unity is....

## install node.js
```
sudo apt-get update -y ; sudo apt-get upgarde -y
sudo apt install nodejs

sudo apt-get update -y ; sudo apt-get upgarde -y
node -v
```

### Note: 
   * simple node.js installation 

------
